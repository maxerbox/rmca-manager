{
  "name": "rmca-manager",
  "version": "1.0.0",
  "description": "The tool to receive the temp from the RMCA Robot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.7.9",
    "electron-rebuild": "^1.6.0"
  },
  "dependencies": {
    "bluetooth-serial-port": "^2.1.5",
    "path": "^0.12.7",
    "react": "^16.1.1",
    "react-dom": "^16.1.1"
  }
}
